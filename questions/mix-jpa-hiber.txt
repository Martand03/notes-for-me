📘 Hibernate, JPA, Maven – Interview Q-Bank
🔹 A. JPA (Java Persistence API)

What is JPA? How is it different from JDBC and Hibernate?

Difference between JPA, Hibernate, and Spring Data JPA.

What are JPA annotations used for entity mapping? (@Entity, @Table, @Id, @GeneratedValue).

What are the different types of relationships in JPA? (@OneToOne, @OneToMany, @ManyToMany).

Difference between unidirectional and bidirectional mapping.

What is the difference between mappedBy and @JoinColumn?

What is the purpose of @Embeddable and @Embedded?

What is the difference between JPQL and SQL?

How do you create a custom query using @Query annotation?

What is a native query in JPA? When would you use it?

What is the difference between EntityManager and Session?

How does JPA handle transaction management?

What are the advantages of using JPA?

What is the difference between first-level and second-level cache in JPA?

What is the N+1 select problem in JPA? How do you solve it?

🔹 B. Hibernate

What is Hibernate? How does it work internally?

Difference between Hibernate and JPA.

What are Hibernate configuration files (hibernate.cfg.xml, properties)?

Explain Hibernate Session and SessionFactory.

Difference between get() and load() in Hibernate.

What are lazy loading and eager loading in Hibernate?

What is cascading in Hibernate (ALL, PERSIST, MERGE, REMOVE, DETACH)?

Explain Hibernate caching (1st-level cache, 2nd-level cache, query cache).

What caching providers are supported in Hibernate (Ehcache, Infinispan)?

What is dirty checking in Hibernate?

Difference between merge() and update().

Difference between transient, persistent, and detached states in Hibernate.

What are Hibernate interceptors and event listeners?

What is HQL? Difference between HQL and JPQL.

How does Hibernate handle batch processing?

🔹 C. Maven

What is Maven? Why is it used?

Difference between Maven and Gradle.

What is the role of the pom.xml file?

What are Maven dependencies? How are they managed?

What are Maven build lifecycle phases (clean, validate, compile, test, package, verify, install, deploy)?

What are Maven plugins? Give examples.

What is the difference between compile, provided, runtime, and test dependency scopes?

What is the purpose of the dependencyManagement tag?

How does Maven resolve version conflicts?

What is a Maven snapshot vs release version?

What is the difference between local, central, and remote Maven repositories?

How do you create a multi-module Maven project?

What is the difference between Maven and Ant?

How do you add custom repositories in Maven?

How do you skip tests during Maven build? (mvn install -DskipTests).

🔹 D. Advanced & Scenario-Based

How do you implement pagination in JPA/Hibernate?

How do you handle optimistic vs pessimistic locking in JPA/Hibernate?

How do you improve performance of Hibernate queries?

How would you debug the SQL queries generated by Hibernate?

How do you integrate Maven with CI/CD pipelines (Jenkins, GitHub Actions)?

How do you handle a large dataset migration using Hibernate + Spring Batch?

What is the difference between Criteria API and JPQL?

How do you use Maven profiles for environment-specific builds?

How do you reduce the size of a Maven build?

How do you integrate Maven with Docker/Kubernetes deployments?
